# Disabled: pull:figma and build:tokens are not implemented in package.json.
# Re-enable after adding those scripts and updating git paths (see src/styles/, not src/scss/ or tokens/).
name: Figma Sync (disabled)

on:
  workflow_dispatch: {}

jobs:
  sync:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Figma Sync is disabled."
