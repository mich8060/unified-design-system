# Choosing layout

Authenticated product → `AppShell` + `Menu`.
Master-detail → `listview` prop.
Settings/form → single main column, max readable width.
Marketing/docs → may omit AppShell; still use tokens.

## See also

- ../patterns/navigation.md
