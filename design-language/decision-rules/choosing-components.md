# Choosing components

Consult `ai/uds-contract.json` `componentCatalog` before inventing UI.

First-party emphasis: `Status`, `Medallion`, `Badge`, `Menu`, `AppShell`.
Forms: `Field` + input family. Actions: `Button`. Data: `Table`, `Item`.

## See also

- ../decision-rules/choosing-patterns.md
