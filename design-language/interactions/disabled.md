# Disabled

Use native `disabled` on inputs and `disabled` prop on buttons.

Disabled elements are not focusable and should not be the only way to convey validation errors.

## See also

- ../interactions/validation.md
