# Selected

Selection: menu `activeId`, table row selection, tabs active state, listview item highlight.

One clear selected state per control group; align with `Menu` and `Tabs` APIs.

## See also

- ../patterns/navigation.md
