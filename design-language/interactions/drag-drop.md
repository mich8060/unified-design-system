# Drag and drop

Use `FileUpload` / `FileUploadCards` for file intake—not custom dropzones unless required.

Resizable panels: `Resizable` component for in-page split layouts (not AppShell listview).
