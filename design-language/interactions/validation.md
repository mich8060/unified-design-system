# Validation

Field-level errors: `Field` + invalid state on `Input`/`Select` with helper text.

Form-level summary only for multi-field issues; don't duplicate every error twice.

## See also

- ../accessibility/forms.md
- ../patterns/forms.md
