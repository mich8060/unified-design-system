# Filters

Place filters above list/table in a horizontal toolbar or left column—not hidden unless space-critical.

Use `Select`, `Checkbox`, date inputs; show active filter count when many filters apply.
