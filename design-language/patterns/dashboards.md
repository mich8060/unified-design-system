# Dashboards

Recipe: `ai/recipes/workspace-dashboard.md` · Example: `ai/examples/workspace-dashboard.tsx`

Compose: `AppShell` + `Menu`, `SectionHeader`, metric `Card`s, `Badge`/`Medallion`/`Status`.

## See also

- ../examples/dashboards.md
