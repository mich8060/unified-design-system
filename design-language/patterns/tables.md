# Tables

Use UDS `Table` for sortable/filterable data. Pair with `SectionHeader` and toolbar actions.

Row actions: icon buttons with labels or dropdown menu—not ambiguous icons alone.

## See also

- ../examples/tables.md
