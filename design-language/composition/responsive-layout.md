# Responsive layout

AppShell menu collapses at product breakpoints; main column scrolls independently.

Listview master-detail: only `.appshell--main` scrolls; list body uses `data-slot=appshell-listview-scroll`.

See `ai/guides/appshell-navigation.md` and `ai/recipes/detail-with-listview.md`.

## See also

- ../patterns/detail-pages.md
