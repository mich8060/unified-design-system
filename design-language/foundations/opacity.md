# Opacity

Use opacity for disabled and subtle secondary chrome—not as a primary color strategy.

Disabled controls should use component `disabled` states (which include token-backed opacity) rather than ad-hoc `opacity-50` on arbitrary nodes.

## See also

- ../interactions/disabled.md
