# Motion

Motion should be **functional**: open/close overlays, expand/collapse, loading feedback.

Prefer component-built transitions. Avoid decorative animation on data-heavy operational screens.

Respect `prefers-reduced-motion` when adding custom motion.
