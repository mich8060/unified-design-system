# Color contrast

Text and interactive elements must meet WCAG contrast against their background.

Use semantic text tokens on semantic surfaces—don't place `--uds-text-secondary` on busy tinted backgrounds without verification.

## See also

- ../foundations/color.md
