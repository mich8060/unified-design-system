# Screen readers

Provide accessible names: visible labels, `aria-label` only when visible label isn't possible.

Icon-only buttons require `aria-label`. Status changes should use live regions where appropriate.
