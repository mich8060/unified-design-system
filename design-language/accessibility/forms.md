# Accessible forms

Every input has a label (`Field`, `Label`). Errors linked via text proximity and invalid state.

Required fields indicated consistently; don't rely on color alone.

## See also

- ../patterns/forms.md
- ../interactions/validation.md
