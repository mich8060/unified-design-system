# Before and after

## Before (anti-pattern)
Custom sidebar div + unstyled buttons + hardcoded `#0075AB` headers.

## After (UDS)
`AppShell` + `Menu` + `Button variant="default"` + `Card` + semantic tokens.

See `anti-patterns/` for common migrations.

## See also

- ../anti-patterns/layout-mistakes.md
