# Component hierarchy

Shell (`AppShell`) → regions → sections (`SectionHeader`) → surfaces (`Card`) → controls.

Don't skip levels: e.g. don't put bare inputs directly on page background without `Field`/section context.

## See also

- ../composition/hierarchy.md
