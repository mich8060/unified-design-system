# Dropdown

## Structure Compliance

- Uses semantic token styles from `var(--uds-...)`.
- BEM naming with `.uds-dropdown` as the block prefix.
- Variants and defaults are defined in `Dropdown.spec.ts`.
- Public exports are limited to component and types through `index.ts`.
- Use `placeholder` for in-field text; `label` is not supported.
