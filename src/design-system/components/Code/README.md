# Code

Syntax-highlighted code renderer powered by PrismJS and UDS semantic code tokens.

## Structure Compliance

- Uses semantic token styles from `var(--uds-code-*)`.
- BEM naming with `.ds-code` as the block prefix.
- Variants and defaults are defined in `Code.spec.ts`.
- Public exports are limited to component and types through `index.ts`.
