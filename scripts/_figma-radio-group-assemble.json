{"fileKey":"3bTua8rojOOC7tYWIEWJl0","skillNames":"figma-generate-library,figma-use","description":"Build UDS radio-group","code":"let code = '';\nfor (let i = 0; i < 12; i++) {\n  const part = figma.root.getSharedPluginData('uds', 'build' + i);\n  if (!part) break;\n  code += part;\n}\nif (!code.length) return { error: 'no staged build code' };\nconst AsyncFunction = Object.getPrototypeOf(async function () {}).constructor;\ntry {\n  return await new AsyncFunction(code)();\n} catch (e) {\n  return { error: String(e), codeLen: code.length, head: code.slice(0, 120), tail: code.slice(-120) };\n}"}