# Expected signals

- `AppShell` + `Menu` in `menu` (not `Sidebar*`)
- KPI tiles use `Card` and `Medallion`
- Alert strip uses `Status`
- At least one panel uses `Item` / `ItemGroup`
- Notifications-style panel uses `Tabs` with line list variant
- imports remain on the package root
