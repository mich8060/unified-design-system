# Expected signals

- `AppShell` + `Menu` in `menu` (not `Sidebar*`)
- Two-column provider layout (fixed left rail + main column) at large breakpoints
- Identity rail uses `Avatar` / `Badge` / `Card`
- Main column uses `Steps` (or documents MicroCalendar / Accordion as allowed variants)
- Single shell only — no nested `AppShell` on the page
- imports remain on the package root
